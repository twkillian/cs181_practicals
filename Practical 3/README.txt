Procedure:
1. Install musicbrainzngs Python Package
	$ conda install -c https://conda.anaconda.org/auto musicbrainzngs

Notes:
train_user_genre_avg_play.csv contains:
user_id, genre, avg number of plays over artists of that genre that the user listened to
